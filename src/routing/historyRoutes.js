/**
 * * Routes for history
 */
const historyRoutes = {
  dashboard: {
    base: '/dashboard',
    sales: '/dashboard/sales',
    customers: '/dashboard/customer',
  },
  items: '/items',
  login: '/login',
};

export default historyRoutes;
